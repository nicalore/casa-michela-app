import 'package:flutter/material.dart';

// The "Gestione del calendario" project phase has not started yet: the
// automatic scheduling algorithm has no backend support at this point, so the
// section is deliberately empty until it does.
class CalendarTab extends StatelessWidget
{
  const CalendarTab({super.key});

  @override
  Widget build(BuildContext context)
  {
    return const SizedBox.shrink();
  }
}
